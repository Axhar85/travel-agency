# Root .env — read by docker-compose.yml only (local Postgres/Redis containers).
# App-level secrets live in apps/api/.env and apps/web/.env.local instead.

POSTGRES_USER=malik
POSTGRES_PASSWORD=malik_dev_password
POSTGRES_DB=malik_travel
POSTGRES_PORT=5432
REDIS_PORT=6379
